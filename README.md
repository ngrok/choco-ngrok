# ngrok chocolatey package

This repository packages the ngrok agent for the [chocolatey package manager](https://community.chocolatey.org/packages/ngrok) on Windows.

## Installation

```
choco install ngrok
```

run from the command line or PowerShell.
